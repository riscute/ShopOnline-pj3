@model IEnumerable<ShopOnline.Models.CategoryProduct>

@{
    ViewBag.Title = "Index";
}

<head>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<h2>Index</h2>
<body>
    <section class="bg-light">
        <div class="container">
            <div class="text-center">
                <h2>WareHouse</h2>
            </div>
            <div class="row justify-content-center">
                <div class="col-md-8 text-center">
                    <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#reg-modal">
                        Updates WareHouse
                    </button>
                </div>
            </div>
        </div>
    </section>
    <div class="modal fade" id="reg-modal" tabindex="-1" aria-labelledby="modal-title" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal-title">Get Last Updates </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div model-body>
                    <p>Hello</p>
                    <label for="modal-email" class="form-label"></label>
                </div>
            </div>
        </div>
    </div>

</body>
<p>
    @Html.ActionLink("Create New", "AddCategoryProduct") 
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CateID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CateName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CateID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CateName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>

